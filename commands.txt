For github:
   git status
   git add .
   git commit -m "commit message"
   git push origin main

For google cloud:
   gcloud run services describe gotravel --region=us-central1
      For Status Check
   gcloud run deploy gotravel --source . --platform managed --region us-central1 --allow-unauthenticated
      For saving/deploying